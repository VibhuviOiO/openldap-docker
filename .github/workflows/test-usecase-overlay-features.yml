name: Test Use-Case - Overlay Features

# This workflow is called by docker-publish.yml after image build
# Can also be triggered manually for testing
on:
  workflow_call:
    inputs:
      image_tag:
        description: 'Docker image tag to test'
        required: false
        type: string
        default: 'latest'
  workflow_dispatch:
    inputs:
      image_tag:
        description: 'Docker image tag to test'
        required: false
        default: 'latest'

jobs:
  test-overlay-features:
    name: Test Overlay Features Use-Case
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker
        uses: docker/setup-buildx-action@v3

      - name: Pull image
        run: docker pull ghcr.io/vibhuvioio/openldap:${{ inputs.image_tag || github.event.inputs.image_tag || 'latest' }}

      - name: Run Overlay Features test
        run: |
          chmod +x use-cases/overlay-features/test.sh
          use-cases/overlay-features/test.sh ${{ inputs.image_tag || github.event.inputs.image_tag || 'latest' }}

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: overlay-features-logs
          path: |
            use-cases/overlay-features/logs/
          retention-days: 3

      - name: Cleanup
        if: always()
        run: |
          cd use-cases/overlay-features
          docker compose down -v || true
